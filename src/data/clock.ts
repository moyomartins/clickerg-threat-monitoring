/** Frozen account snapshot: fixtures, date filters and relative labels share this clock.
 * Deliberately does not advance on reload or wall-clock time. */
export const NOW = Date.parse('2025-08-15T09:00:00Z');
